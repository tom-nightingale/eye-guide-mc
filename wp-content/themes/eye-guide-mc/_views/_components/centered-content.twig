<div class="container py-24 text-center max-w-screen-2xl">

    <div class="relative after:absolute after:top-1/2 after:left-0 after:w-full after:h-[1px] after:bg-gray-200 mb-8">
        <h1 class="relative z-10 inline-block px-6 mx-auto mb-0 bg-white">{{heading}}</h1>
    </div>

    <div class="max-w-4xl mx-auto copy">

        {{content}}

    </div>

    {% if hasButton %}
        <a href="{{buttonDestination.url}}" class="mt-8 btn btn--tertiary">{{buttonText}}</a>
    {% endif %}

</div>